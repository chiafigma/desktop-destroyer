{
  // Tests run in Node, so they need @types/node — which the main and ui projects
  // both deliberately exclude. Hence a third project rather than widening either.
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "lib": ["ES2020"],
    "types": ["node"]
  },
  "include": ["src/shared/**/*"]
}
